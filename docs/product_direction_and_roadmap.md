# 端侧 AI 业务方向重新梳理（Week 3 后期）

背景：Week 3 想做的"文档动态分类"折腾了很多轮，反复验证下来，"把一堆文件自动整理成一个整洁、数量少的分类目录树"这个具体目标，用端侧小模型做不稳，而且跟前沿云端 agent（Cowork）对比后发现，连它也不是靠"判断更准"做到的，是靠绕开这个问题（读文件夹结构）做到的——说明这不只是端侧能力不够，是这个目标本身的形状可能就不太适合"一堆独立判断收敛成一个小体系"这条路。这份文档是借这个契机，把"端侧 AI 到底能稳定交付什么体验"重新梳理一遍，往上抬一层看整体业务方向，不是就事论事继续磨分类这一件事。

---

## 现状速览（新接手的 session 先看这一节）

**⚠️ 2026-09-07 更新，回头复查 8-31 那次地基审计记的 6 条"新发现"，逐条对照这周实际做的东西——5 条已修复并有真实数据验证，1 条部分修复：**

1. **文件生命周期管理/增量索引 —— 已修复**：统一支持多目录/多盘共享数据库（`target_dir`/`volume_label` 字段），`index`/`tag` 都支持增量（内容没变跳过）+ `--force`（强制全量重跑）。真实踩过并修掉两个具体 bug：`/tmp` 是 macOS 软链接导致路径前缀匹配静默失效（改用 `.resolve()`）、SQL `LIKE` 通配符转义（文件夹名带 `_`/`%` 会被误当通配符）。
2. **语义搜索暴力线性扫描 + 没有关键词检索 —— 部分修复**：稠密检索改成 numpy 批量矩阵乘法（不再是 Python 逐个循环算余弦相似度）；新增关键词检索（SQLite FTS5，trigram 分词 + bm25 排序）+ RRF 融合排序，真实语料验证过（"投资建议"能靠关键词精确命中，"工程师思维"能靠标签级信号弥补稠密检索的部分不足，两路证据都有）。**仍然不是真正的近似最近邻索引结构**（本质还是暴力扫描，只是常数因子小了很多），语料量级再涨一个数量级需要重新评估；**也仍然没有像打标签那样补一次正式验收集**——见下面新增的用例，供自己验证用。
3. **表格类内容切片丢表头 —— 已修复**：新增 `chunk_tabular_text()`，按工作表边界切块，每个 chunk 都带表头，不只是第一个 chunk，真实 xlsx 验证过（842/842 chunk 正确）。
4. **Week 3 资源监控空白 —— 已修复(内存/CPU)**：双进程监控（steward 自己 + llama-server 分开测，两边内存开销是独立的两笔账），真实全量跑验证过（steward 自身峰值内存 ~935MB，llama-server 峰值 ~13.5GB）。**GPU 算力这一路仍然没测**，是主动决定不测（不想要 sudo 输入密码的体验代价），不是遗漏——Apple Silicon 统一内存架构下，内存这块已经间接覆盖了大部分实际需要回答的问题。
5. **打标签管线崩溃安全性脆弱 —— 已修复**：去掉了无条件清空旧数据的 Step 0、去掉了误导性的嵌套事务包装（`with index.connection:` 不提供真正的整体回滚，已用最小复现例子验证过），改成 upsert 语义，中途崩溃不会比运行前更差。
6. **文本提取范围 —— 更牢了**：又修了 chunk 切分断词 bug（英文单词被从中间切断）、markdown 图片/链接噪声清理，以及本轮最大的一处新发现——**大文件/JSON 代表性抽样问题**：一份 118MB 的 JSON 聊天记录导出文件曾导致索引耗时 101 分钟、且打标签摘要完全不具代表性（只反映了文件最早 0.0025% 的内容）。定位到两个独立的根因（chunk 数量爆炸 + 打标签摘要抽取只读开头）后，最初的修法是给 JSON 数组单独做"按记录抽样"，但**用真实的 118MB 文件实测对比后，发现这个 JSON 专属预处理反而让最终覆盖面变差**（1151 条记录只覆盖 180 条/15.6%，不做预处理直接靠统一的 chunk 数量上限兜底反而能覆盖 362 条/31.5%）——所以最终改成对所有格式统一处理，不再有 JSON 专属分支。索引耗时从 101 分钟降到 546 秒，真实验证摘要现在能跨两年时间线均匀取样，不再卡在最早一条记录上。**PDF 里约 18-20% 没有可提取文字层（纯扫描/字体缺 ToUnicode 映射）这个缺口依然没解决**，需要 OCR，明确搁置。
7. **语义搜索容易"为了结果而给结果"、`--top-k` 截断掉看不见的其余结果 —— 已修复**：新增最低相关度门槛（`_MIN_DENSE_SCORE = 0.5`，稠密检索原始余弦相似度低于它直接不进结果）+ 标签作为第三路 RRF 信号（新增 `document_tags_fts`，文档整体标签也能参与检索，补上"主题分散在全篇、没集中在某个 chunk 里"这个盲区）。用 8 个真实查询词测过：6 条干净（`不动产登记`/`微服务`/`日本签证`/`财务凭证`/`报销发票`/`工程师思维`），1 条部分好（`收入证明`，头部对、尾部混进弱相关发票），**1 条真实翻车**（`银行流水`：稠密检索把一份 SQL migration 教程的原始相似度（0.545）排到很高，清过了 0.5 门槛——最初以为是"压过了一份真正相关的发票（0.4746）"，靠标签信号把那份发票捞了回来；但后续换 pymupdf 重新提取+重新打标签后发现，那份发票其实是普通餐饮发票，文本变干净后 LLM 不再给它"银行流水"标签，说明最初那个标签是读乱码文本时的不精确猜测——**语料里可能根本没有真正意义上的"银行流水"类文档，这份 SQL migration 教程的假阳性依然清过门槛被当成"找到了"**，性质上更接近"语料里没有相关内容却没被门槛正确拦住"，不是"假阳性压过真阳性"）。诊断出翻车的根因不是"专业词汇普遍不可靠"（`不动产登记` 同样生僻但结果完美），而是"流水"这个词本身有歧义（银行流水/流水线/流水号）容易把向量模型搞混，是个具体、可解释的个例，不是系统性问题。**这个 0.5 阈值明确记录为"当前证据支撑的估计值，跟具体 embedding 模型和语料内容分布绑定，不是通用常数"**，换模型或语料大幅变化需要重新用真实查询校准（曾尝试用"这次查询自己的分数距全库均值几个标准差"做动态阈值，实测效果不如绝对值，没有采用）。附带新增 `tags --tag <关键词>` 命令，补上"穷举式列出所有真正带这个标签的文档"这个能力缺口（`search` 是排序找最相关的几个，会被门槛/top-k 过滤，不保证完整；这个命令直接读 `document_tags_fts`，不加载 embedding 模型，用户自己验证过"基本很准"）。
8. **PDF 提取器从 pypdf 换成 pymupdf**：上面第 7 条测试时撞见几份发票 PDF 提取出来是乱码（泰米尔文/古吉拉特文这类跟文档内容毫不相关的字符），查了字体结构，根因是这批 PDF 用 Identity-H 编码的内嵌字体、ToUnicode 映射缺失或不完整，`pypdf` 解析失败后瞎猜出了乱码——**这跟"PDF 完全没有文字层，只能靠 OCR"是不同类型的问题**：那类是真没救（两个库测都是空的），这类是能修的，实测把提取器换成 `pymupdf` 之后同样文件能正确还原成汉字（字体处理更健壮，能从字体文件自己内部的映射表兜底），`报销相关` 目录下 64 份真实发票交叉测试没有发现回归。附带许可证考量：pymupdf 是 AGPL/商业双授权，现阶段本地个人工具不受影响，以后如果谈产品化分发，这是需要重新评估的一个点，不是可以忽略的细节。
9. **语义搜索架构再升级：接入 cross-encoder 重排序，从"一个阈值硬防守"改成"先粗筛后精排"**：上面第 7 条那个 `_MIN_DENSE_SCORE` 阈值方案，本质上是让稠密检索一个信号身兼"捞候选"和"判断相关性"两职，`银行流水` 案例已经暴露过它的脆弱性。查证确认这是 RAG 领域的已知问题（bi-encoder 向量相似度不是为精确相关性判断训练的），2026 年的生产标准是"retrieve-then-rerank"两阶段架构。落地了本地 cross-encoder `bge-reranker-v2-m3`（`reranker.py`，跟 embedding 模型同代码路径加载）：三路检索（向量+关键词+标签）不再对相似度做硬性门槛，只负责粗筛出候选池（默认 80 个，`--candidates` 可调），候选池统一交给重排序模型精排，`_MIN_DENSE_SCORE` 撤掉，改成重排序自己的分数门槛 `_MIN_RERANK_SCORE`。**过程中真实踩过一次回归**：第一版阈值 `-5.0` 按两个最极端的"完全不相关"案例校准，没覆盖到"确实不相关但没那么离谱"的中间地带，导致 `露营装备推荐` 重新冒出 19 个不相关结果；改成 `-2.5` 后修复。**进一步查证发现"用绝对阈值卡重排序分数"这个做法本身也是业界公认的脆弱设计**（换模型/换任务，同一个阈值可能完全失效），尝试了 2026 年提出的 TARG 思路（用"第一名和第二名的分差 gap"代替绝对阈值判断），搭好回归测试框架（`tests/`，可反复重跑）实测后**证伪**：`宠物疫苗接种记录`（该判定不相关的假阳性）gap 反而很大，`日本签证`/`财务凭证`（真正相关、只是好答案不止一个）gap 反而接近 0——gap 离不开绝对分数这个前提，单独用会判反，不采用，绝对阈值方案保留为当前基线。2 条已知瑕疵被有意保留在案例池里跟踪（`宠物疫苗接种记录` 假阳性、`投资建议` 排到一份实际是 PTE 英语学习笔记的文档——chunk 级局部关联"投资你自己"这个比喻掩盖了整篇文档的真实主题）。

**后续又测试、又证伪的两条候补方案（2026-09-08），记录下来避免以后重新踩同一个坑：**

- **标签交叉验证信号（`tag_corroboration`）**：观察到这两条已知 bug 有个共同点——命中结果都是"稠密检索单路命中，标签检索完全没搜到这份文档"，猜测"没有标签佐证"可以当一个额外的从严信号。实现成纯诊断字段（不接入过滤逻辑），扩充回归集到 19 条案例（新增 `工资薪金`/`个人信息表`/`MongoDB`/`汽车维修保养记录`/`职业规划建议`，专门设计用来压力测试这个假设），跑出真实分布：**有标签佐证且结果正确 4 / 有标签佐证但结果错误 0 / 无标签佐证但结果正确 8 / 无标签佐证且结果错误 2**。`职业规划建议` 是最关键的反例——18 个结果全部合理相关，但整条查询标签命中数是 0（标签是开放词汇，"职业规划建议"这个查询词跟文档里"职业鼓励"这类标签本来就对不上，trigram 检索天然搭不了这座桥）。**结论：证伪**，"无标签佐证"在这个语料里大量出现在完全正确的结果上，不是稀有情况，拿它做过滤规则会误伤 8 个好结果去换 2 个坏结果，代价不划算，没有采用。
- **LLM-as-judge（直接让本地 LLM 对"查询词 vs 文档片段"做是/否相关性判断）**：先撞上一个技术问题——Qwen3.5-2B 默认会先输出一段思考过程（进 `reasoning_content` 字段，不是 `message.content`），`max_tokens` 太小时思考没写完就被截断，`content` 一直是空字符串；解法是打标签管线里已经在用的同一个参数 `extra_body={"chat_template_kwargs": {"enable_thinking": False}}`，加上之后拿到了正常的"是/否"回答。但拿真实数据一测，暴露出一个新问题：**这个判断对 prompt 措辞极度敏感，调整 prompt 去修一个案例，会把另一个案例带偏**——原始 prompt 下，`投资建议`/`宠物疫苗接种记录` 两个已知 bug 都能正确判"否"，但对照组 `不动产登记`（应该判"是"的最简单案例）却误判成"否"（模型抠字眼："文档里写的是'未查询到不动产信息'，不算真的有不动产信息"）；把 prompt 改得更宽松（"哪怕内容是'查询无结果'，只要事务类型对得上就算相关"）能修好 `不动产登记`，但 `投资建议` 案例又被带回"是"（该文档确实大段讨论"要不要跟风做投资"，宽松判断标准下会被当成相关）。**结论：跟重排序分数阈值调优是同一类脆弱性，只是把"调阈值"换成了"调 prompt 措辞"，没有从根本上解决问题**，而且引入了一个原本不存在的新失败模式（对照组假阴性）。真要做对，需要的是 CRAG 论文里说的那种专门训练/校准过的相关性分类器，不是拿一个通用对话 prompt 简单包一层——这本身就是一个独立的、有自己成本和风险的项目，不是"顺手加一个判断"就能拿到的免费收益，暂不投入。
- **决策：2 个已知 bug（`宠物疫苗接种记录` 假阳性、`投资建议` 主题漂移）继续作为已知的小瑕疵跟踪在回归集里，不再追加新方案去修，把精力挪回更高杠杆的方向。**（Week 4 计划见下方"还没做"小节。）

**诚实的现状评估（2026-09-07，别把"这周做了很多"误当成"这块地基已经稳了"）**：搜索这条链路做了两个阶段的活——**地基阶段**（暴力扫描改矩阵运算、补关键词/标签检索、补相关性判断机制）基本做完了，结构性问题都有了解法；**精调阶段**（阈值校准、候选池策略、已知案例修复、正式验收标准）才刚起步，回归集只有 13 条案例，覆盖面窄，还有 2 条已知瑕疵没解决，候选池上限（80）在语料变大后会成为结构性瓶颈，`_MIN_RERANK_SCORE` 依然是有限样本校准出来的估计值。不该说"语义搜索已经做到位"，更准确的说法是"地基稳了，装修还没完工"。

**决策：暂停继续磨语义搜索，切到去重检测。** 理由：剩下的两个瑕疵都是"越往后修边际成本越高"那类（一个是单个弱假阳性，一个是"比喻用词掩盖整篇真实主题"这种开放式语言理解问题），继续投入前需要先攒更多真实痛点证据，不该无止境地凭现有数据继续调；而去重检测从最早的地基审计就被点名"风险最低、最该优先做"，却一直没动，是明确摆在那的机会成本。

10. **新增精确重复文件检测（`duplicates` 命令）**：只判断"内容 byte 级完全一样"（SHA256 哈希），不做近似相似判断，不做任何删除/移动，纯只读扫描+报告——跟 index/tag 完全独立，不碰数据库，每次都是全新扫描，不做增量（刻意的最小切片，近似相似检测/增量持久化留给下一阶段）。设计上有一个真实的性能优化：先按文件大小分组（`stat()` 现成信息），大小在全目录里独一无二的文件直接跳过、不用读内容算哈希，只对"大小有重复"的那一小撮真正算哈希——全量 `~/Documents`（589 个文件）扫描只用了 0.09 秒（对比 index 的提取链路要 546 秒，因为这次不解析内容，只读字节）。判断只看内容哈希，不看文件名/大小，专门避开了"餐饮1.pdf 在好几个文件夹里同名但内容不同"这个已知的坑；复用 `scan.find_project_roots()` 跳过代码项目内部，避免 node_modules 这类目录产生大量噪声。**验收标准这次可以定得比分类/搜索硬得多**（哈希比对是确定性判断，不是概率判断）：零假阳性（每组报告出来的重复都用系统自带 `shasum -a 256` 独立验证过一致）、正样本（故意造的跨文件夹同内容文件正确分组）、负样本（同名不同内容正确不被误判）、代码项目跳过（伪造项目内部的重复文件正确不出现在报告里）、不改动任何文件（扫描前后哈希比对完全一致）——五条全部通过。真实语料上直接挖出 6 组、13 个文件、约 2.3MB 真实重复（身份证扫描件存了两份、好几份签证材料模板在不同文件夹重复、一份 `global.less`/`global.scss` 内容完全一样），不是构造出来的演示数据。

---

**⚠️ 2026-08-31 更新，本轮做了两件事——文件类型提取范围扩大、以及一次系统性的地基核查（不是新功能，是回头检查已有实现有多稳）：**

1. **文件类型提取范围明显扩大**：新增支持 csv/json（零依赖）、xlsx（新增 `openpyxl`）、xls（新增 `xlrd`；真实语料里 `.xls` 扩展名下其实混着三种不同格式——真正的老版二进制格式、被存错扩展名的 xlsx、以及 SpreadsheetML/Excel 2003 XML 电子表格，已按文件真实内容分流处理，不是只信扩展名）、doc（新增 `olefile`，读内部 `WordDocument` 数据流做启发式文字提取，不是完整格式解析，但真实文件验证内容干净可用）、以及一批零散源码/脚本/配置/日志文件（`.py/.js/.ts/.vue/.java/.go/.rs/.log` 等，复用现成的纯文本提取器，零新代码路径）。详细踩坑记录（SpreadsheetML 里未转义的 `&` 导致 XML 不合法、xlrd 对个别第三方生成文件的已知兼容性限制）见本文档下方"地基审计"表格的提取相关条目。图片/视频的内容理解（OCR/视觉）明确排除在这轮之外，等地基审计的问题处理完再回头看。
1.5. **并发效率讨论（暂不优先做，先记录结论）**：真实测过"泳道从 4 加到 16，总吞吐只提升约 1.6 倍"（记录在 `dynamic_classification_architecture.md` 的技术附注里），说明瓶颈是硬件算力天花板，不是并发槽位数——这意味着"合并多文件到一次请求减少请求数"或者"继续加槽位"这类改动大概率收效有限，而且合并请求还会引入"多份不相关内容在同一次 attention 里互相看得见"的串味风险（这两周吃过同类型 bug 的亏，不想在没验证过的情况下引入新的同类风险）。真正测过、真正有效的方向是**缩短 snippet 长度**：拿 4 篇真实长文档（58K~52万字符不等）对比过 1500 字符 vs 800 字符两种 snippet 长度的打标签输出，结果质量没有实质差别，800 字符是个验证过、随时可用的选项，但按当前优先级（先地基后性能），这次不实施，留作后续参考。
2. **系统性地基核查（六条真实发现，全部读代码+实测验证过，不是猜的）**：见下方"地基审计"表格新增/更新的条目——文件生命周期管理缺失（删除/改名的文件永远不会从索引清除）、`tag`/`index` 命令没有增量机制（每次全量重跑）、语义搜索是暴力线性扫描没有真正的向量索引结构、表格类内容（csv/xlsx）用了为长文章设计的切片策略导致丢失表头上下文、Week 3 打标签管线完全没接入资源监控（GPU 消耗从 Week 2 至今没被测过）、打标签中途崩溃时的持久化机制比代码注释宣称的更脆弱（嵌套事务导致"整体回滚"的保证不成立）。这些还没开始修，先记录下来，下一步要讨论优先级和修法。

**⚠️ 2026-08-27 更新，这是这份文档最新、最重要的一条结论，务必先看：分类体系（category/taxonomy）已经被完整废弃，代码库里不再维护任何分类。** 这几周从 V1（聚类打标签）到 V2（开集逐文件分类）再到 V3（taxonomy 归纳+闭集分类），每一版都花了大量精力打磨、每一版都能用真实数据证明"能力比上一版强"，但每一版最终都在新的真实文件上炸出新的边界案例——直到最后一轮复盘才定位到根因：**这不是哪次没调好参数，是"让每个文件收敛到唯一分类"这个目标本身，跟真实文件天然多归属、边界模糊的性质是冲突的**（比如一篇"用工程师思维聊投资"的文章，本来就该同时挂"投资""工程师思维"这两个标签，不该被逼着二选一）。完整的踩坑记录、三个独立根因（reasoning 生成截断、规则覆盖不检查原始判断、taxonomy 覆盖不到的内容会被强行硬凑）、每一条的真实证据，见 [docs/dynamic_classification_architecture.md](dynamic_classification_architecture.md) 的"最终结论"一节。

**现在的架构**：只保留两样东西——**tags**（开放式、多标签、不受固定列表限制，这几周所有压力测试里唯一没被真实数据打脸过的能力）和 **reasoning**（开放式一句话内容描述，不再是"为分类做依据"）。`categories` 表、`category_id` 字段、`Stage A/A-verify/B-override` 整套机制已经从代码里删除，不是标记废弃、是物理删除，见 `src/steward/semantic_classifier.py` 最新版本。`main.py tag` 命令的 `--structural-base-url`/`--structural-model` 参数也一并删除（是给 taxonomy 归纳这一步单独配大模型用的，这一步已经不存在了）。

后续"整理目录"这个目标不再指望分类体系兜底，改成"tags 聚类"——不在索引阶段强行归类，而是在用户想整理的那一刻现算一次分组建议（算错了重新点一次，不会像强制分类那样写死进数据库、层层传导）。具体聚类原则、以及顺带识别出的几个风险更低的真刚需方向（重复文件检测、安装包清理建议、文档摘要、通用文件名重命名建议），也记在 `dynamic_classification_architecture.md` 的"最终结论"一节里。

以下是这几天更早之前的实际进展，跟下面"一、地基审计"表格里当初写的"待办项"相比，这几条已经从"没做/没测过"变成"做完并验证过"（**注意**：下面这几条里提到"分类""category"的地方，按今天的最终结论已经不存在了，只保留 tags/reasoning 部分仍然成立）：

- **打标签质量验收：已通过。** 按下面"二、打标签验收"定义的参考案例集实测跑过，**零跨类别污染**（唯一硬红线）全部守住，标签具体、相关，人工抽查结果符合预期。当时表格里"打标签质量：需要一份明确的验收标准"这一行，现在已经是**过去式**。
- **非文档类型文件的覆盖缺口：已修复。** 原来 `indexing.py` 只给"支持内容提取"的文件（pdf/docx/txt/md/html）调用 `upsert_document()` 写入数据库，图片/视频/代码/压缩包这类文件完全没有进数据库，是"安静地遗漏"。现在改成扫描到的每个文件都先登记 `documents` 表（带 Week 1 判断出的 `basic_type`），只有提取/分段/向量化这部分才按是否支持格式跳过。同时 `semantic_classifier.py` 新增了一个轻量的"基础类型识别"档位（不读内容、不过 SLM，直接用 `basic_type` 生成 `category`/`tags`），让这些文件也能出现在 `tags` 报告里，标注为"基础类型识别"而不是伪装成语义打标签。真实语料（`~/Downloads` 618 个文件）验证过：全部 618 个文件都进了库，其中 77 份走了深度打标签，538 份走了基础类型识别，3 份未归类。
- **CLI 改名完成**：`classify` → `tag`（打标签），对外措辞同步调整为"深度打标签 / 基础类型识别 / 未归类"三档展示，不再用"分类"这个会让人误以为"每个文件归到一个确定位置"的词。当前命令集是 `index` / `tag` / `tags` / `search`。
- **`unclassified` 门槛机制变了**：最初设计是"置信度 < 0.70 打回未归类"，这条经过实测证明不可行（SLM 自报的置信度几乎恒定在 0.95 附近，跟内容质量无关，是噪声不是信号），已经正式废弃。现在的未归类只由两个客观条件触发：内容长度过短（预过滤）、SLM 返回内容解析失败——**不再依赖模型自报的置信度做门槛判断**。
- **架构从"聚类后打标签"（V1）彻底转向"逐文件独立打标签"（V2）**：V1 遗留的 `semantic_clusters` 表、`documents.cluster_id` 字段/外键、`save_semantic_cluster()` 方法已经从 schema 里整个删掉（不需要迁移，因为 `steward.db` 本来就不进版本控制，重新跑一次 `index` 就是干净状态）。

**还没做、下一步建议做的（2026-09-07 更新）：**
- **语义搜索——地基已稳，精调阶段的具体活还没干完**（详见地基审计表格"语义搜索"那一行的完整记录），具体是：
  - `宠物疫苗接种记录`（假阳性）、`投资建议`（chunk 局部关联掩盖整篇主题）两条已知案例还挂在回归集里没修。
  - 候选池上限（80）是结构性瓶颈，语料继续变大会更明显，不是调参能绕开的。
  - `_MIN_RERANK_SCORE=-2.5` 依然是有限样本校准出来的估计值。
  - 回归集扩充到 19 条案例（16 条严格判定 + 3 条仅记录），最新一轮 14/16 通过，覆盖面比之前宽了不少，但仍未到"正式验收通过"的程度，2 条已知瑕疵见上面第 9 条末尾的记录，**已决定不再继续投入新方案去修，转向更高杠杆的方向**（标签交叉验证、LLM-as-judge 两条候补方案都测过、都证伪了，见上面第 9 条）。
  - 每次查询 8~16 秒延迟，CLI 工具能接受，做成实时交互产品需要重新评估。
- **图片/视频的内容理解——完全空白**，是当前最大的范围缺口。跟另一条线在测的"本地小模型跑图像识别"是同一个方向的地基预研，等那边有结论可以回来决定要不要接进主线。
- ~~去重检测——技术门槛低（向量/哈希都是现成的），是之前审计就点名"最该优先做的低风险新方向"，一直没动，候选优先级最高。~~ **精确重复检测（byte 级哈希）已完成**，见上面第 10 条。**近似重复检测——2026-09-08 讨论后明确决定暂缓，不是技术做不到**：
  - **文档近似重复**：跟 V1 聚类共享同一个工具（embedding 相似度），但目标区间不一样——聚类要在"话题相关"这个连续、拥挤的中间地带划线（这正是 V1 反复炸出边界案例的根源），近似重复瞄的是"基本就是同一份东西"这个稀疏的极端高相似度区间，理论上好判得多，判错代价也低（是可忽略的提示，不是被坐实的分类结论）——**跟聚类不是同一个难度级别的问题，不该因为"用的工具一样"就类比成"同一个坑"**。真正暂缓的原因是另外两条：这个功能的真实价值还没验证过（精确重复已经把"同一文件存了好几份"这个最常见场景覆盖掉了，近似重复能新增多少真实案例是未知数）；加上刚经历完语义搜索一整天的阈值调试，这个时间点上没必要立刻再开一个同类型的新战场。
  - **图片近似重复**：技术上可行（感知哈希，不需要模型推理），但这个项目现在对图片内容是完全空白的（Week 1 至今只有扩展名/magic bytes 判断"这是个图片"，从没打开过图片内容），图片这块要做，绕不开"要不要开始做图片内容处理"这个更大的决定，不该在做近似重复的时候顺带被动做出这个决定，等图片索引/识别的思路想清楚了再回头看。
- "散落文件 vs 已整理文件夹"显式建模、深层嵌套/复杂对象识别、行为/文件移动层——这些都还没开始，属于明确排除在当前阶段之外的后续工作，不是被遗漏。

**Week 4 方向盘点（2026-09-08 讨论过程，含被否决的候选，记录理由避免以后重新提议）：**
- **RAG 自然语言问答——讨论后决定暂不做，不是能力做不到，是时机不对。** 这是 CLAUDE.md 里 Layer-1 候选列表原本就点名的方向（复用检索+本地 LLM，不是另起功能），一度是 Week 4 的第一候选，但用户提出三个疑问倒逼重新想清楚：(1) RAG 这个方向已经太卷，担心做不出差异化；(2) 用户问跟语料完全不相关的问题时怎么处理；(3) 现在做会不会影响以后扩展图片/视频。结论：(2) 是决定性的——RAG 的生成层会在检索层已经不完美的地基上（2 个已知 bug、候选池上限、有限样本校准的阈值）再叠一层"生成是否忠实于检索内容"的新可靠性问题，而且 LLM 面对语料外的问题时会倾向于安静地退回自己的预训练知识作答，而不是老实说"没找到"——这正是 RAG 领域一个有名的、需要专门设计和测试的失败模式，跟检索层"怎么知道语料里真的没有相关内容"是同一个根问题的更难版本，两个都没解决之前不该叠加。(3) 判断为中性——现在做不会明显加速、也不会明显阻碍图片/视频（当前本地 LLM 是纯文本模型，两边复用的基础设施有限）。**结论：RAG 问答暂缓，等检索层这两个已知 bug 处理到更有把握的程度，或者出现更明确的真实需求信号再重新评估。**
- **敏感文件识别、安装包识别——讨论后否决，价值不够清晰。** 最初提议是"风险分层里第三层的候选"，被用户直接质疑"有啥意义"后重新审视：敏感文件识别检测出来之后，steward 当前的能力边界里并没有配套的下一步动作（不像重复文件检测，检测完"删一份"是显而易见的下一步）；安装包识别（判断"已装完可以删"）存在真实的正确性风险——没有更深的操作系统集成（读注册表/Applications 目录反查是否已安装），没有可靠信号判断"删除是安全的"，最多只能是"供你自己看一眼，不代表任何安全保证"，价值和风险不成正比。**两者都不再是 Week 4 候选。**
- **当前 Week 4 唯一在推进的方向：把语义搜索这 2 个已知 bug 的处理方式定下来（已完成，见上面第 9 条末尾"决策"）——接下来腾出手做的下一个具体方向留待下次对齐时再定。**

---

## 一、地基审计（截至 Week 3 后期，如实写，不护短）

**重新定位**：分类结果（category）不直接展示给用户，只作为后续行为/检索的内部参考信号——这意味着"归一化后有多少个分类名"从验收红线降级成了次要指标，真正的红线只有一条：**内容判断不能串味**（不相关内容不能被塞进同一类/同一堆标签）。这条红线目前验证下来是守住的。

| 地基 | 状态 | 说明 |
|---|---|---|
| 文件类型识别（Week 1，扩展名+magic bytes）| **牢** | 纯规则、零 LLM 参与，风险最低的一块。 |
| 文本提取（Week 2 起）| **牢，范围已明显扩大** | pdf/docx/txt/md/html/csv/json/xlsx/xls/doc + 常见代码/脚本/配置/日志文件，几百个真实文件几乎零提取失败；**仍然完全不覆盖图片/视频的内容理解**（OCR/视觉），这批文件只走 Week 1 的类型识别，是明确的范围缺口，不是可靠性问题——2026-08-31 还额外发现：**约 18-20% 的真实 PDF 文件内部根本没有可提取的文字层**（要么是纯扫描件、要么字体缺 ToUnicode 映射），实测换了两个不同的 PDF 库结果一致，这不是提取器的 bug，是这批文件本身的性质，要解决只能靠 OCR，跟图片/视频的缺口是同一类问题。**2026-09-07 又发现并修复了一类不同的问题**：语义搜索测试时撞见几份发票 PDF 提取出来是乱码（泰米尔文/古吉拉特文这类跟文档内容毫不相关的字符）——查了字体结构，这批 PDF 用 Identity-H 编码的内嵌字体，`pypdf` 解析 ToUnicode 映射失败后瞎猜，猜出了乱码；这**不是**"没有文字层"那类真没救的问题，实测换成 `pymupdf` 之后同样文件能正确还原成汉字（字体处理更健壮，能从字体文件自己内部的映射表兜底），已经把 PDF 提取器整体从 pypdf 换成 pymupdf，`报销相关` 目录下 64 份真实发票交叉测试没有发现 pymupdf 更差的案例。需要留意：pymupdf 是 AGPL/商业双许可，现阶段本地个人工具不受影响，以后如果要产品化分发，这是需要重新评估的一个点。 |
| 单文件内容判断（分类/打标签的核心）| **牢** | 反复拿刁钻案例（简历 vs 教程、话题混杂的聊天记录）验证过，跨语料没有退步过。 |
| 文件夹整体识别 | **半牢** | 验证过几个真实案例（Nest课程、发票、PTE、对话List）都判对了，只覆盖"直接父目录"这一种情况，深层嵌套/复杂结构（代码项目、解压出来的目录）还没处理。 |
| Taxonomy 归一化（分类名数量）| **不再是地基问题** | 按新的定位，降级为次要指标，不阻塞任何事。 |
| **语义搜索** | **地基已稳，精调阶段刚起步——不算"到位"** | 2026-09-07（当天分两阶段做的）：**上午**——稠密检索改成 numpy 批量矩阵乘法；新增关键词检索（FTS5 trigram+bm25）+ 标签检索（`document_tags_fts`，第三路 FTS5 索引，粒度是整份文档不是 chunk）三路 RRF 融合；新增稠密检索最低相关度门槛（`_MIN_DENSE_SCORE=0.5`）。8 个真实查询词测出 6 条干净、1 条部分好、1 条真实翻车（`银行流水`——稠密检索把不相关内容排到比真正相关文档更高）。**晚上**——查证发现这类问题在 RAG 领域是已知问题（bi-encoder 向量相似度不是为精确相关性判断训练的），业界标准解法是"retrieve-then-rerank"两阶段架构，2026 年依然是生产标准（搜到多篇 2026 年文章确认）。落地了 `bge-reranker-v2-m3`（本地 cross-encoder，跟现有 embedding 模型同厂商同代码路径加载，见 `reranker.py`）：三路检索不再对稠密相似度做硬性门槛，只负责"粗筛出候选池"（默认 80 个，`--candidates` 可调），候选池统一交给重排序模型精排，`_MIN_DENSE_SCORE` 整个撤掉，改成重排序自己的分数门槛 `_MIN_RERANK_SCORE`。**验证结果**：`银行流水` 案例被正确修复（假阳性从原始相似度 0.545 排第一，变成重排序 -10.9 分垫底，语料里最接近的真实内容排到了前面）；调试过程中真撞过一次回归——第一版阈值 `-5.0` 是按两个最极端的"完全不相关"案例（-11 附近）校准的，没覆盖到"确实不相关但没那么离谱"的中间地带（-3~-4 分），导致 `露营装备推荐` 这个语料里没有的话题重新冒出 19 个不相关结果；改成 `-2.5`（贴着已知最差的真正相关案例）后重测，`银行流水`/`露营装备推荐`/`报销发票`/`工程师思维`/`不动产登记`/`微服务` 六个案例全部正确。**代价**：每次查询加载重排序模型 + 打分候选池，总耗时从原来 3~7 秒涨到 8~13 秒（重排序本身 100~120 毫秒一对，80 个候选约 3~10 秒）。**仍然没有正式固定的回归集**；**候选池上限（80）意味着 `search` 命令不适合"穷举某个概念下所有文件"这类需求**（真实相关文档超过候选池大小时，排在后面的永远没机会被重排序看到），这类需求应该用 `tags --tag`（穷举式标签筛选，不受候选池限制）；`_MIN_RERANK_SCORE=-2.5` 依然是"当前证据支撑的估计值"，不是通用常数，换 reranker 模型或语料话题分布大幅变化需要重新校准，详见 semantic_search.py 里的注释。**后续搭了正式的回归测试框架**（`tests/search_regression_cases.json` + `tests/run_search_regression.py`，13 个真实案例，一键重跑），并用它验证了"用 gap（第一名和第二名分差）代替绝对阈值"这个思路——**证伪**，`宠物疫苗接种记录` 这个假阳性案例 gap 反而很大，`日本签证`/`财务凭证` 这类真正相关、好答案不止一个的案例 gap 反而接近 0，gap 离不开绝对分数这个前提，单独用会判反，绝对阈值方案保留为基线。回归集当前通过 10/12（2 条已知瑕疵故意保留跟踪，见下面"还没做"）。**结论：搜索的结构性地基问题已经解决，但精调阶段（阈值精度、候选池策略上限、已知案例修复、验收标准覆盖面）才刚起步，不该称为"到位"。** |
| **打标签质量** | **已验收，PASS** | 用参考案例集实测过，零跨类别污染，标签具体相关，详见下面"二、打标签验收"和"现状速览"。 |
| 去重检测 | **精确重复已完成，近似重复未做** | 2026-09-07：新增 `duplicates` 命令（`duplicates.py`），SHA256 哈希判断内容 byte 级是否完全一致，只读不改动文件，不做增量/持久化。判断依据只看内容哈希，不看文件名/大小（专门避开"同名不同内容"的坑）；先按大小分组再对有重复大小的文件才算哈希，性能优化过。验收标准比分类/搜索硬（确定性判断）：零假阳性(系统 shasum 独立验证)、正样本、负样本、代码项目正确跳过、扫描不改动文件，五条全部通过。真实语料（全量 `~/Documents`）0.09 秒扫完，挖出 6 组 13 个文件、约 2.3MB 真实重复。**近似重复（图片感知哈希、文档内容相似但不完全一样）还没做**，是下一阶段候选。 |
| "散落文件 vs 已经在用户文件夹里"的显式建模 | **完全没做** | 未来行为层要用到的关键信号，现在系统里没有任何字段记录这个区分。 |
| 复杂对象识别（深层嵌套/解压产物）| **完全没做** | 跟"文件夹整体识别"是同一个原则的延伸，还没实现。 |
| **非文档类型文件的数据库覆盖** | **已修复** | 图片、视频、代码、压缩包这些文件现在都会被登记进数据库并打上基础类型标签（`basic_type`），但仍然**不做语义层面**的内容理解，只是不再"安静地遗漏"，详见"现状速览"。 |
| **文件生命周期管理 / 增量索引**（2026-08-31 发现，2026-09-07 修复）| **已修复** | 统一多目录/多盘共享数据库（`target_dir`/`volume_label`），`index`/`tag` 都支持增量（内容没变跳过重新处理）+ `--force`（强制全量重跑）；幽灵清理（`mark_missing_as_absent`）只在当前 `target_dir` 子树范围内把这次没扫到的旧记录标记为不存在，不会误伤其它目录/盘的记录。真实修过两个具体 bug：`/tmp` 是 macOS 软链接导致路径前缀匹配静默失效、SQL `LIKE` 通配符转义缺失。 |
| **表格类内容的切片（chunking）策略**（2026-08-31 发现，2026-09-07 修复）| **已修复** | 新增 `chunk_tabular_text()`，按工作表边界（`[sheet: xxx]` 标记）切成独立表格块，每个 chunk 都带上所属块的表头（不只是第一个 chunk），真实 553999 字符的 xlsx 验证过（842/842 chunk 正确）。 |
| **资源监控覆盖率**（2026-08-31 发现，2026-09-07 修复内存/CPU 部分）| **已修复(内存/CPU)，GPU 仍未测** | 新增双进程 `ResourceMonitor`（steward 自己的进程 + llama-server 进程分开测，两边内存开销是独立两笔账，只测一边会丢另一半信息），真实全量跑验证过（steward 峰值内存约 935MB，llama-server 峰值约 13.5GB）。GPU 算力（`powermetrics`）仍未测，是主动决定不测（不接受每次可能弹 sudo 密码框的体验代价），Apple Silicon 统一内存架构下内存这块已经间接覆盖了大部分实际需要回答的问题。 |
| **打标签管线的崩溃安全性**（2026-08-31 发现，2026-09-07 修复）| **已修复** | 去掉了无条件清空旧数据的 Step 0、去掉了误导性的嵌套事务包装（用最小复现例子验证过 `with index.connection:` 嵌套 `with self.connection:` 不提供真正的整体回滚），改成 upsert 语义——中途崩溃（比如 llama-server 挂了）不会比运行前更差，只是"这次还没处理到的文件保留旧标签，不会被清空"。 |
| **大文件/JSON 代表性抽样问题**（2026-09-07 新发现并修复）| **已修复** | 一份 118MB 的 JSON 聊天记录导出文件曾导致索引耗时 101 分钟、且打标签摘要完全不具代表性（只反映文件最早 0.0025% 的内容）。最初的修法是给 JSON 数组单独做"按记录抽样"，但用真实文件实测对比后发现这个 JSON 专属预处理反而让最终覆盖面变差（1151 条记录只覆盖 180 条/15.6%，不做预处理、直接靠统一的 chunk 数量上限兜底反而能覆盖 362 条/31.5%）——改成所有格式统一处理（完整原文切分 → chunk 数量上限 → 落库前文本体积上限），不再有 JSON 专属分支。索引耗时降到 546 秒，真实验证摘要现在能跨两年时间线均匀取样。 |

## 二、打标签验收：参考案例回归集（本轮优先做这个）

把今天验证用过的真实案例固定成一份可以反复重跑的小型回归集，不再靠肉眼重新扫报告判断"这次是不是比上次好"：

- **案例池**：简历 ×3、AI 聊天记录 ×5（音乐/投资/技术/旅行等不同话题）、发票 ×2、证件材料 ×1，后续可以再扩。
- **通过标准（按优先级）**：
  1. 零跨类别污染——不相关内容不能被塞进同一类/同一批高分标签，这条是唯一的硬红线。
  2. 每个案例的分类结果，人工看一眼认为合理，不要求措辞完美。
  3. 每个案例排名靠前的标签里，看不到明显不相关的词。
  4. 分类名数量不再是硬指标，只要求不随文件量线性暴涨。
- 以后改动分类/打标签逻辑，就拿这份集合重新跑一遍对照这四条，而不是重新肉眼扫全量报告。

**语义搜索和标签质量都建议照这个方法补一次验收**——挑几个已知"应该搜得到"的真实案例，定义"排在前几名才算过"，现在完全没做过。

### 语义搜索测试用例（2026-09-07，供自行验证用，还不是正式固定的回归集）

用 `python main.py search "<query>" --top-k 8` 逐个跑，每条验证的目的不一样：

| 查询词 | 验证目的 | 期待看到什么 |
|---|---|---|
| `投资建议` | 关键词检索（FTS5）能不能精确命中字面短语 | 之前验证过：能精确找到含这个短语的文档，排名靠前 |
| `存钱理财的方法` | 稠密向量检索能不能靠语义相关性命中，即使字面不完全重合 | 应该能找到投资/理财相关内容，哪怕文档里没有逐字出现这句话 |
| `报销发票` | 关键词+语义两路应该同时命中、互相印证 | 排名靠前的应该是报销相关目录下的真实发票/申请单 |
| `工程师思维` | 之前分析过的一个真实边界案例 | 会看到 `工程师的投资建议.txt`（真实带这个标签）排第几名，是不是靠前；可以对照之前聊过的"标签能不能当第三路信号"这个未决问题自己判断 |
| `理财` | trigram 分词的已知限制——少于 3 个字符，关键词那一路完全没法匹配 | 关键词检索应该 0 命中，只剩稠密检索单独工作，可以观察这种情况下排序是不是还合理 |
| 一个语料里明显没有的话题（比如 `露营装备推荐`） | 检查会不会硬凑结果、分数是不是明显偏低 | 不应该出现看着像是硬凑的高分结果 |

跑完把 `dense_hit_count`/`sparse_hit_count`/融合排序分数贴给我，一起看要不要正式固定成回归集。

**已经用真实语料跑过一轮结果（2026-09-07，加了相关度门槛+标签第三路信号之后）：**

| 查询词 | 结果 |
|---|---|
| `工程师思维` | 干净——加了标签信号后，真实带这个标签的 `工程师的投资建议.txt` 排到第 1 名（之前没有标签信号时排第 2） |
| `露营装备推荐` | 正确返回"没有找到足够相关的内容"，三路命中数全是 0 |
| `报销发票` | 干净，top8 全部是真实发票；`document_count` 正确报出 85 个结果通过门槛，不是只有 8 个 |
| `不动产登记` | **完美**——三路（向量/关键词/标签）全部命中同一份文档，只有 1 个结果，就是对的那份 |
| `微服务` | 完美，top8 全部是真正讲微服务的技术内容 |
| `日本签证` | 很干净，top8 全部是真实签证材料 |
| `财务凭证` | 干净，top8 全部是真实发票（但其中 2 份提取文本是乱码——后来定位到是 PDF 提取器的问题，见上面"PDF 提取器从 pypdf 换成 pymupdf"那条，跟搜索排序本身无关） |
| `收入证明` | 部分好——第 1 名完全正确，第 2-4 名合理相关，第 5-8 名开始混进弱相关的发票噪声 |
| `银行流水` | **真实翻车，且后来发现比最初判断更值得警惕**——最初测的时候，有一份发票（`get-pdf.pdf`，当时提取文本还是乱码状态）带着"银行流水"标签，被标签信号捞了回来，跟一份不相关的 SQL migration 教程（原始相似度 0.5450）并列第一；换成 pymupdf 重新提取+重新打标签之后，这份发票的文本变干净了（其实是一张普通餐饮发票，附带收款方银行账号），LLM 重新判断后**不再给它"银行流水"这个标签**——说明最初那个标签本身很可能是读乱码文本时的不精确猜测，不是稳健判断。重新测：`标签检索命中: 0`，语料里可能根本没有真正意义上的"银行流水"类文档，但那份 SQL migration 教程（0.5450）依然清过了 0.5 门槛被当成"找到了"展示出来——**这比最初"假阳性排到真阳性前面"的判断更糟：很可能是"语料里根本没有相关内容，但门槛没兜住这个假阳性"**，性质上更接近 `露营装备推荐`，只是门槛没能正确识别出来。诊断"流水"一词歧义（银行流水/流水线/流水号）容易带偏向量模型这一点依然成立，不是"专业词汇"形状普遍有问题（`不动产登记` 反例）。 |

8 条里 6 条干净、1 条部分好、1 条有具体可解释的翻车原因——目前证据支持"这套组合拳基本站得住"，`银行流水` 这类案例值得继续留意但不构成推翻现有设计的理由，暂不改动排序算法本身。

**接入 `bge-reranker-v2-m3` 重排序之后重新测过一轮（2026-09-07 当天晚些时候）：**

| 查询词 | 重排序后的结果 |
|---|---|
| `银行流水` | **修复**——之前排第一的 SQL migration 教程彻底消失，top1 变成一份提到"电子签章流水号"的个税清单，其余是带银行账号信息的发票，都是语料里能找到的、跟"银行"沾边程度最高的真实内容 |
| `露营装备推荐` | 第一版阈值（-5.0）下**出现回归**：冒出 19 个不相关结果（App 版本号 JSON、Nest 教程等）；调紧到 -2.5 后修复，正确返回"没有找到" |
| `报销发票` | 干净，top3 全部是真实报销单/发票，62 个通过重排序（候选池 80） |
| `工程师思维` | 干净，top1 精确命中，9 个通过重排序 |
| `不动产登记` | 干净，top1 精确命中（不动产信息查询结果单），4 个通过重排序 |
| `微服务` | 干净，top3 全部是真正讲微服务的技术内容，19 个通过重排序 |

重排序模型加载 0.8~5 秒（首次加载慢，后续应该会有系统级文件缓存）、80 个候选打分 3~5 秒，每次查询总耗时从原来的 3~7 秒涨到 8~13 秒。

## 三、打标签对语义搜索有没有帮助

有，但不是直接替代关系，是两件事：
1. **标签可以作为搜索的一个独立信号**，跟向量相似度搭配用（比如查询词直接命中某个标签，可以在排序时额外加权）——这是常见的"稠密向量+关键词/标签"混合检索思路，标签质量好，这条路才走得通。
2. **标签服务的是"浏览"，搜索服务的是"提问"**，是两种互补的交互方式，不是谁替代谁。
3. 但两者共享同一套底层（文档向量+embedding 模型），验证标签质量能**部分**建立对搜索质量的信心（至少证明向量空间本身是可用的），不能完全替代——搜索还有自己独有的环节（query 向量化、chunk 级排序），需要单独测。

**结论**：能稳定对外交付体验的，是"找得到"（搜索）和"认得出、能筛选"（打标签），不是"自动摆放整齐"（分类目录）。这不是能力不够，是要交付的东西选错了形状。

---

## 二、Week 3 目标与交互形态：建议怎么调

### 从"分类"改成"打标签"，不只是换个词

"分类"这个词本身在暗示"每个文件归到一个确定的位置"——一旦文件被放错，用户体验是"AI 出错了、不可用"，容忍度很低。"打标签"暗示的是"辅助信息，允许不是每个都 100% 精确，用来筛选/搜索足够用"——同样的底层准确率，用户对"某个标签不太贴切"的容忍度，远高于对"文件被塞进错误文件夹"的容忍度。今天一整天验证出来的东西，恰好直接支持这个产品化结论：不是我们的判断变差了，是"分类"这个交互形态对错误零容忍，"标签+搜索"这个形态天然更宽容。

### 建议的 CLI 形态

```
steward index <目录>        # 建索引（已有）
steward search <query>      # 语义搜索（已有）
steward tag <目录>          # 原 classify，建议改名并调整对外描述：产出多标签，不强调"分到一个类"
steward duplicates <目录>   # 新增：重复/相似文件检测
```

`tag` 命令的输出（2026-08-27 更新）不再带"主分类"字段——这条当时是预判，后来真实做了几周验证，发现"主分类"这个概念本身就是问题所在，不只是"该不该主推"的问题，已经被完整移除，见上面"现状速览"。对外展示/宣传主推的是"打了哪些标签、能不能搜到"，这条判断被后续的真实数据完整印证了。

---

## 三、端侧 AI 业务亮点 brainstorm（结合公司 toC 存储产品定位）

公司做 toC 存储产品，用户的数据文件本来就活在盘/本地目录里——这个前提本身就是端侧 AI 最自然的战场，不需要说服用户"把数据交给端侧处理"，数据已经在那了。按用户能直接感知到的价值分成四组：

### A. 找得到（Find）
- **端侧语义搜索/问答**：已有，文本文档场景成立。以后如果接一个轻量的图文联合 embedding 模型，同一套向量检索基础设施可以直接复用到图片语义搜索（"去年夏天在海边拍的照片"），不需要重新设计架构。

### B. 认得出（Recognize / Tag）
- **文档多标签**：已验证。
- **图片语义标签**（人物/风景/文档扫描/截图/宠物/美食……）：存储设备的典型用户手里有大量照片视频，"识别照片内容、按人物/地点/事物搜索"是几乎所有云相册产品的核心卖点（Google 相册、iCloud 相册），但那些都是上传云端处理的。**端侧版本是真正的差异化**：不用上传就能搜，而照片视频恰好是用户最不愿意上传云端的数据类型之一——隐私是硬卖点，不是营销话术。

### C. 清得爽（Clean / Optimize）
- **重复/相似文件检测**：技术门槛低（图片用感知哈希、文档用已有向量相似度），可以直接续着现有代码做，不依赖今天纠结的分类收敛问题。直接呼应存储产品的核心价值——"你的硬盘里有 3.2GB 重复照片"是很有说服力的演示效果，而且是"省空间"这个存储公司天然在乎的叙事。
- **闲置大文件/安装包识别**：同样低门槛，同样贴合"存储优化"这个产品定位。

### D. 保得住（Protect / Backup Awareness）
- **敏感文件识别与保护提醒**：检测身份证、合同这类敏感文件是不是明文存放，提示要不要加密/隔离。端侧处理这类数据用户完全没有心理负担，云端做这个用户会本能抵触；直接呼应"数据在你自己的设备上，安全由你自己的设备负责"这个存储硬件公司最核心的品牌叙事。
- **重要文件缺备份提醒**：某类文件（证件、合同）只有一份、没有第二个副本时主动提醒。这是需要**持续常驻监控本地设备**的场景——云端 agent 没有持续访问用户本地多块物理盘的能力，也没有动机去做这件事，这是结构性地做不到，不是暂时做得不够好。

### E. 跨盘协同（Cross-Drive）—— 最贴合公司业务模式、云端完全替代不了的方向
- **多盘/多设备去重与归档建议**（"这块新盘里的照片，一半已经在老盘备份过了，要不要清理"）。用户买多个存储设备（NAS + 移动硬盘 + U盘）本来就是这类公司的核心商业模式，端侧 AI 天然适合做"看看我这几块盘上都有什么、重不重复、该怎么归档"——**这是云端 agent 结构性做不到的**（它没法同时挂载访问用户的多块物理设备），是这几组里最值得重点标出来讲给领导听的方向，因为它不是"我们比云端做得好一点"，是"这件事本来就只有端侧能做"。

---

## 四、为什么要往这几个方向走，不是继续死磕分类

云端 agent 的优势：模型能力强、算力不受限、能做开放式的复杂推理和多步骤操作。它的天然弱点：处理隐私敏感内容有用户心理门槛（照片/证件/财务文件用户本能不想上传）、没法持续常驻监控本地多台设备、每次调用都有真实的算力和网络成本。

端侧的优势正好卡在它的弱点上：本地隐私敏感数据、持续后台监控、零上传门槛、离线可用。业务亮点应该往"云端做不了、或者用户不愿意让云端做"的方向找，不是往"云端本来就做得比我们好"的方向死磕——今天在"自动整理成一个整洁分类目录"这件事上撞的墙，恰好就是一个云端更擅长的方向（它靠强大的通用推理能力硬啃，端侧硬啃不动），继续磨这个方向性价比很低。

---

## 五、这一轮"打地基探路"建议交付什么

给产品/领导看的，不需要现在做全，建议按"验证过的 vs 值得写进方向"分两层：

**现在就能包装成 CLI 演示的**（技术已经在手上，最多再加一个 `duplicates` 命令）：
- 语义搜索（已有）
- 文档打标签（已有，建议调整对外措辞）
- ~~文档去重检测（新增，技术门槛低）~~ **精确重复检测已完成**（`duplicates` 命令），近似重复检测还是候选

**值得作为"下一步方向"讲清楚、但不需要这轮就实现的**：
- 图片语义标签与搜索（需要接入轻量视觉模型）
- 敏感文件保护提醒、备份完整性检测（需要设计"持续监控"的运行模式，跟现在"手动跑一次命令"的交互形态不一样）
- 多盘协同（需要设计跨设备的数据模型，是这几个方向里最有公司特色、但也是架构改动最大的一个，适合放在"愿景"那页 PPT，不适合放在这轮 CLI 验证范围内）
