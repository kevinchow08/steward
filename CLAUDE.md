# CLAUDE.md — 端侧文件分类 Agent

这是给 Claude Code 的项目上下文文件。请先读完这份文档再开始写代码。

## 项目背景

领导(Jiewen)批准的原型项目:探索端侧模型处理多类型文件分类的能力表现和资源消耗,并希望未来能产品化。这是一个开放性探索项目,没有预定义的分类标准、也没有预定义的产品形态,具体怎么设计、怎么研发,由用户自己决策。采用迭代式开发——先跑通最小切片拿到反馈,再逐步增强,不追求一开始就设计完备。

开发按周迭代,每周一定两个目标:**一个当周能做完、可汇报的小目标**,和**一段给领导看的方向性大目标**(不需要马上实现,只是讲清楚往哪走)。

长期方向是把它做成一个真正的 agent(分类之后能自主决策文件该怎么处理),但决策/行动层要等分类+索引这条链路验证扎实之后再碰。

## Week 1(已完成)

**目标:** 跑通"文件类型分类"这一层——不使用任何模型,纯靠扩展名 + magic bytes 把文件分成 document / image / code / archive / executable / media / data 等基础类型。

**做完的东西:**
1. 一个能跑通的最小 pipeline:遍历目录(`scan.py`)→ 分类(`classifiers/rule_based.py`)→ 资源监控(`monitor.py`)→ 输出结果(`report.py`)
2. 在真实语料(`~/Downloads` 353 个文件、`~/Documents` 567 个文件)上跑通,unknown 占比从最初的 36.5% 收敛到个位数
3. 延迟/内存/CPU 基线数据(`output/baseline.json`)
4. 踩出的关键坑,后续阶段要记得:
   - OOXML/ODF 格式(docx/xlsx/pptx 等)底层是 zip 容器,magic bytes 跟 archive 撞车,靠"magic bytes 判成 archive 时用扩展名二次确认"解决,这个逻辑是通用的,以后加新的 zip 系格式(apk/jar/ipa)不用改代码,只用改配置
   - 隐藏文件/目录判断(`.` 前缀)是 macOS/Unix 专属约定,以后要上 Windows 得换成读文件属性位
   - mp4/mov/m4a 的 magic bytes 不在文件开头(在 offset 4 的 `ftyp` 标识),现在的前缀匹配逻辑读不出来,靠扩展名兜底,这是已知的能力缺口,不影响当前正确性

**诚实的边界:** 这一层不涉及模型,所以它答不了"端侧模型能力表现如何"这个核心问题,只是地基。汇报时不要把这一步包装成"验证了模型能力"。

## Week 2(当前阶段 —— 现在只做这个)

**小目标(这周要跑完、能汇报的):** 建立只读、本地运行的 document 内容理解与语义索引最小闭环。

Week 1 回答了"文件是什么技术类型"；这一周要验证的是"端侧模型能否根据文档内容，找回语义相关的文件"。这是一条直接服务产品化的地基：后续的标签、摘要、检索和整理建议都要复用文本提取与语义索引能力。

1. 从一次新的 Week 1 扫描结果中筛出可进入内容索引路线的文件：`basic_type == "document"`，以及 CSV/TSV 等结构化数据文件
2. 提取第一批支持格式(pdf/docx/txt/md/html/csv/tsv/xlsx)的文本；不支持、无文字层或提取失败的文件要如实记录状态和原因，不静默丢弃
3. 用一个本地 embedding 模型把文本片段生成向量，并连同文件元数据保存为本地索引
4. 提供最小的自然语言检索：输入一句查询，返回最相关的文件路径、匹配片段和相似度，供人工判断是否有用
5. 记录文本提取覆盖率、索引耗时、模型加载/向量化耗时、内存与 CPU 基线；GPU/ANE 测量视本周实际模型运行方式决定

**这一周的验收不是“模型已经会自动整理文件”，而是：** 对一组人工准备的真实查询，能看到相关文档是否出现在靠前结果中；同时能清楚说明失败、未知和不支持的文件。

**聚类的定位：** embedding 聚类可以在索引完成后作为辅助分析工具，用来观察真实文档是否存在稳定主题、是否值得沉淀标签或类别；它不是本周产品交付，也不把 `cluster_03` 之类的内部编号当成永久分类标准。候选类别表若产生，只是可迭代的观察记录，绝不强迫新文件落入已有类别。

**明确排除(这周不做,不要主动扩展):**
- 不做自动语义分类或自动生成正式标签；类别和标签体系先在真实检索结果上逐步定义
- 不做 image 的 OCR、聚类或路由(先把 document 这条走通)
- 不做对话式 RAG、Web UI、向量数据库服务或跨机器同步
- 不碰决策/行动层(移动/重命名/打标签写回文件系统都不做)

## 后续方向(现在只需要知道,不要现在就实现)

### 分类链路
- **语义索引与检索**(Week 2)—— 文本提取 + 本地 embedding 是后续能力的共享基础；先验证用户能否通过内容找回文件，再判断分类粒度
- **标签与结构化语义分类**(核心验证目标)—— 分类是一个文件的主要业务归属，标签是可并存的多个检索特征。基于真实语料和检索反馈逐步定义类别/标签，由模型输出 JSON(带 confidence 和 `unclassified` 回退)，不把新文件硬塞进既有类别
- **跨文件关联聚类**(更后面，作为发现关联与候选标签的工具，而非用户可见的固定分类体系)

### 产品化方向(领导要求产品化,具体形态由用户自己设计,以下是候选,按风险分层)
- **第一层(只读,可全自动)**:打标签/建索引(自建索引,比如 SQLite,不依赖 macOS Finder 标签——用户不常用 Finder 标签)、生成摘要(展示方式待定:CLI 命令 / 写入 Spotlight 备注 / 本地 web 面板)、自然语言检索(RAG 问答,复用聚类阶段的文本提取+embedding 基础设施,不是另起的功能)
- **第二层(可逆,建议先"提议+用户确认"再自动化)**:移动归档、重命名、重复文件检测
- **第三层(有损,只建议不自动执行)**:清理建议(比如装完的安装包提示
- 删除)

### agent 决策/行动层(更更后面)
在分类 + 索引这条链路验证扎实之前不碰。核心风险是"高置信度的误判"一旦接上自动行动会导致真实破坏,所以行动层要先有 confidence 分级和用户确认机制打底。

Week 1/2 的架构设计要为这些后续阶段留好扩展口子(比如分类规则用配置文件而不是硬编码,输出格式要方便后续阶段复用),但不要现在就把这些功能实现出来。

## 技术栈约束

- **运行环境:** MacBook Pro,M4 Pro,48GB 统一内存,本地开发
- **语言:** Python 3.x(跨平台考虑,后续阶段要接 llama.cpp 生态,Python 生态兼容性最好)
- **依赖策略:** 能不加依赖就不加。Week 1 只用了 `pyyaml` + `psutil`;Week 2 开始会加文本提取库(pypdf/python-docx/beautifulsoup4)和一个本地 embedding 模型/推理库,这是必要的新增,不算过度设计
- **资源监控:** 不要用 PowerShell 方案(那是之前 Windows 机器上 iSA 测试用的 `monitor_process.ps1`,专属 Windows),这里用 Python + psutil 重新实现同等能力(采样进程内存 RSS + CPU 占用,记录耗时)。GPU 占用 Week 1 不测(不涉及模型推理),Week 2 上模型之后要考虑怎么测(psutil 不支持,可能要靠 powermetrics)
- **代码风格:** 简洁优先,不要过度设计。分类规则(扩展名/类别映射)放配置文件里,不要硬编码在代码逻辑中,方便后续迭代扩充而不用改代码

## 测试语料

用户本地真实杂乱文件(不是专门构造的干净测试集),这样才能真实反映"端侧模型/pipeline 在真实场景下"的表现。目前已经在 `~/Downloads`(353 个文件)和 `~/Documents`(567 个文件)上验证过。

## 协作方式

用户偏好:
- 一步一步来,先讨论方案再动手,不要一次性生成一大堆代码
- 遇到问题就报告实际报错信息,不需要过度预判所有边界情况
- 不喜欢过度设计和不必要的抽象层
- 想自己动手写、理解每一部分逻辑,不是要一个开箱即用的黑盒
- 第一次做 agent 开发,Python 不是主力语言,大部分能看懂但不熟——写代码时倾向多加解释性注释帮助理解,即使这跟"默认不写注释"的通用代码风格冲突,这个项目是特例
- 按周迭代:每周一对齐"这周能做完的小目标"+"给领导看的方向性大目标",讨论清楚了再动手
