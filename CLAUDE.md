# CLAUDE.md — 端侧文件分类 Agent

这是给 Claude Code 的项目上下文文件。请先读完这份文档再开始写代码。

## 项目背景

领导(Jiewen)批准的原型项目:探索端侧模型处理多类型文件分类的能力表现和资源消耗。这是一个开放性探索项目,没有预定义的分类标准,采用迭代式开发——先跑通最小切片拿到反馈,再逐步增强,不追求一开始就设计完备。

长期方向是把它做成一个真正的 agent(分类之后能自主决策文件该怎么处理),但**现阶段完全不碰决策/行动层**,只做分类。

## 当前阶段范围(Week 1 —— 现在只做这个)

**目标:** 跑通"文件类型分类"这一层——不使用任何模型,纯靠扩展名 + magic bytes 把文件分成 document / image / code / archive 等基础类型。

**这一阶段要拿到的东西:**
1. 一个能跑通的最小 pipeline:遍历目录 → 分类 → 输出结果
2. 语料的类型构成分布(为下一阶段设计语义分类标准打基础)
3. 一份延迟 + 资源占用(内存)的基线数据

**明确排除(这周不做,不要主动扩展):**
- 不涉及任何模型推理(文本模型、多模态模型都不碰)
- 不做语义分类(比如区分"发票 vs 合同")
- 不做 embedding 聚类
- 不做跨文件关联
- 不做任何自动化文件操作(移动/重命名/归档)
- 不做 UI,命令行脚本 + 输出文件即可

**诚实的边界:** 这一层不涉及模型,所以它答不了"端侧模型能力表现如何"这个核心问题,只是地基。汇报时不要把这一步包装成"验证了模型能力"。

## 后续阶段(现在只需要知道方向,不要现在就实现)

- **Week 2+:探索性 embedding 聚类** — 对已分类文件(尤其是 document 类)跑聚类,人工看真实内容自然分成几堆,归纳出语义分类标准候选
- **Week 2+:结构化输出语义分类**(核心验证目标)— 文本文件用纯文本模型 + 预定义类别 prompt 输出 JSON;图片文件用多模态模型
- **更后面:跨文件关联聚类**
- **更更后面:agent 决策/行动层**

Week 1 的架构设计要为这些后续阶段留好扩展口子(比如分类规则用配置文件而不是硬编码,输出格式要方便后续阶段复用),但不要现在就把这些功能实现出来。

## 技术栈约束

- **运行环境:** MacBook Pro,M4 Pro,48GB 统一内存,本地开发
- **语言:** Python 3.x(跨平台考虑,后续阶段要接 llama.cpp 生态,Python 生态兼容性最好)
- **本阶段依赖:** 只需要 `pyyaml`(配置文件解析)+ `psutil`(资源监控),不引入任何 ML 相关依赖,能不加依赖就不加
- **资源监控:** 不要用 PowerShell 方案(那是之前 Windows 机器上 iSA 测试用的 `monitor_process.ps1`,专属 Windows),这里用 Python + psutil 重新实现同等能力(采样进程内存 RSS,记录耗时)
- **代码风格:** 简洁优先,不要过度设计。分类规则(扩展名/类别映射)放配置文件里,不要硬编码在代码逻辑中,方便后续迭代扩充而不用改代码

## 测试语料

用户本地 Downloads 文件夹里的真实杂乱文件(不是专门构造的干净测试集),这样才能真实反映"端侧模型/pipeline 在真实场景下"的表现。50 个文件左右起步。

## 交付物(这一阶段结束时应该有的东西)

1. 一个可运行的项目脚手架(目录结构由你和用户协作确定,不要照抄任何既定模板)
2. 一份分类结果输出(逐文件明细 + 类别分布汇总)
3. 一份延迟/内存占用基线报告
4. 结果里要能看出多少文件判断不出类型(fallback/unknown),方便判断规则要不要扩充

## 协作方式

用户偏好:
- 一步一步来,先讨论方案再动手,不要一次性生成一大堆代码
- 遇到问题就报告实际报错信息,不需要过度预判所有边界情况
- 不喜欢过度设计和不必要的抽象层
- 想自己动手写、理解每一部分逻辑,不是要一个开箱即用的黑盒
